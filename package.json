{
  "name": "haief",
  "type": "module",
  "private": true,
  "description": "HAIEF — Human & AI ElevAItion Foundation public site (Astro on Cloudflare)",
  "license": "(Apache-2.0 AND CC-BY-4.0)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "check": "astro build",
    "deploy": "npm run build && wrangler deploy",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.3",
    "@astrojs/sitemap": "3.6.1",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
